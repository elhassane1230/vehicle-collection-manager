# Documentation

- `diagramme.png` — UML class diagram (add your exported PNG here; it is
  referenced from the main README).
- `screenshots/` — application screenshots for the README.

If you use ObjectAid/Eclipse, you can also keep the `.ucls` source here.
